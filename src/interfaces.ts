export interface ICrew {
  name: string;
  agency: string;
  image: string;
  wikipedia: string;
  launches: string[];
  status: string;
  id: string;
}

export interface IRocket {
  image: string;
  name: string;
  wikipedia: string;
  id: string;
}
